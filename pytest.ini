[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

log_cli = true
log_cli_level = INFO

addopts = --alluredir=allure-results

markers =
    platform: restrict test to a specific platform (ios or android)
    smoke: quick sanity checks
    regression: full regression suite